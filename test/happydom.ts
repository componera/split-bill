/**
 * Happy DOM preload script for Bun test runner.
 * Provides DOM simulation for React component tests.
 */
import { GlobalRegistrator } from "@happy-dom/global-registrator";

GlobalRegistrator.register();
